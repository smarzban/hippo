# Hippo Roadmap — Hardening & Features (2026-06-12)

Canonical action list for the post-v1 work. Detailed per-feature plans live alongside this
file as `2026-06-12-<feature>.md` (written when we brainstorm each item).

## Where we are
- v1 merged; **PR #2** (independent-review fixes: M1 connection lock, M2 safe reindex, M3 model
  stamp, M4 tool-call knob, M5 CORS, M7 citation resolution, L3/L4) merged to `main`.
- **Two independent reviews** done. Findings + my verdicts: `/tmp/hippo-review-response.md`
  (review #1) and `/tmp/hippo-review-2-assessment.md` (review #2). Both fold into the items below.
- A filename-as-title experiment was built and **discarded** (heading-based titles retained).
- Suite: 57/57, zero-network. Runs on local Ollama (chat/enrich `gpt-oss:120b-cloud`,
  embeddings local `nomic-embed-text` 768-dim).

## Prioritized backlog

| # | Item | Tier | Round | Status |
|---|------|------|-------|--------|
| 1 | Google SSO auth + roles | Foundation | **now** | **built** (PR pending) |
| 2 | `/sources` path allowlist + git-repo feed | Foundation | **now** | **built** (PR pending) |
| 3 | Production-readiness (checklist below + Docker) | Foundation | **now** | **merged** (PR #4) |
| 5 | Word (`.docx`) parsing — PDF dropped to future | Feature | **now** | **merged** (PR #5) |
| 6 | MCP **server** (token auth; Claude Code + Desktop) | Feature | **now** | **built** (PR pending) |
| 4 | Scale: 1000s docs / 40–50 users (Postgres+pgvector, pooling, LLM concurrency) | Foundation | next | deferred |
| 7 | Slack integration | Feature | next | **built** (branch `build/slack-integration`; design: `../specs/2026-06-13-slack-integration-design.md`) |
| 8 | MCP **client** / connectors (Google Drive etc.) | Feature | next | deferred |
| 9 | Settings UI | UX | next | **built** (branch `build/settings-ui`; design: `../specs/2026-06-13-settings-ui-design.md`) |
| 10 | Deploy at Acme (k8s manifests, IAP wiring, git-sync sidecar, probes) | Foundation | next | deferred |
| 11 | MCP OAuth for claude.ai web connectors (builds on `oidc` mode) | Feature | later | deferred |
| 12 | PDF parsing (docling or similar structure-recovering extractor) | Feature | later | deferred |

**Design for items 1,2,3,5,6:** `../specs/2026-06-12-team-readiness-design.md`
(decided 2026-06-12: 3 auth modes `iap`/`oidc`/`none`; `users` table with
developer/manager/admin; source-level access enforced in `Storage`; two git repos as the
doc feed + upload-to-repo via GitHub Contents API; Docker yes / k8s manifests deferred;
`.docx` via mammoth, PDF dropped; MCP mounted at `/mcp` with personal access tokens).

## This round — plan, then implement one after another: 1, 2, 3, 5, 6

### 1 Auth (Google SSO) + 2 `/sources` allowlist  — one work item
Implement `verify_request` (currently a no-op stub), gate the proxied routes, restrict
`/sources` to configured allowlisted roots, decide the `host: true` exposure posture.
Open forks to settle in brainstorm:
- Google identity model: raw Google OAuth vs an OIDC library vs a managed gateway.
- Who's allowed in: Google-Workspace domain allowlist vs explicit user list.
- Credential model: session cookies (browser UI) vs bearer tokens (Slack/MCP/CLI).

### 3 Production-readiness — concrete checklist (from the two reviews)
- **Ingestion limits** (M2/L7): cap upload bytes, per-file bytes, max doc chars pre-enrichment,
  max files+bytes per sync; reject before decode/embed; oversized → `skipped` not `failed`.
- **Grounding enforcement** (M3 — core trust promise): (a) delimit + label tool output as
  untrusted *data*; (b) lightweight post-stream check (≥1 citation when factual claims made +
  cited paths exist in retrieved set) surfaced as a soft UI warning, NOT hard reject/rewrite;
  (c) prompt-injection test fixtures. (Full reject/rewrite validator deferred — research-grade.)
- **grep hardening** (M4): keep regex, cap pattern length + wall-clock timeout (`regex` module);
  do NOT make literal-only. (Lock change already moved the scan outside the DB lock.)
- **Chunk overlap fix** (L5): re-check `len(tail)+len(text)+sep <= max_chars` after prepending
  overlap; regression test for near-limit adjacent paragraphs.
- **Drawer HTTP-error fix** (L6): check `r.ok` in `DocDrawer.tsx`; keep `doc` null on failure.
- **`hippo backup` command** (L7): `VACUUM INTO` for a consistent single-file snapshot; document
  WAL-safe backup.
- **CI**: GitHub Actions running `uv run pytest` + `npm run build` on PRs.
- **Also** (review #1 deferred): logging/observability, dependency version bounds, serve the
  built UI (`ui/dist`) from FastAPI instead of the Vite dev server.

### 5 PDF/Word parsing
Independent feature slice. Extend `parsers.py` SUPPORTED + add extractors (pdf, docx → markdown).
Pairs with the ingestion-size limits above (these files are large).

### 6 MCP server (expose Hippo to Claude/other harnesses)
Wrap the agent's existing tools (search / read_document / list_documents / grep) as an MCP server
so external MCP-speaking agents can query the knowledge base. Mostly independent; auth matters for
scoping once multi-user.

## Next round (after this one)
4 Scale (Postgres+pgvector, connection pooling, LLM concurrency/rate-limit handling) ·
7 Slack · 8 MCP client/connectors · 9 Settings UI.

## Working agreement
Brainstorm 1,2,3,5,6 together first; then implement them one after another (Fable for the
design/brainstorm phase; faster model OK for mechanical implementation). Scale + remaining
features come after.
